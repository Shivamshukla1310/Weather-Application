document.getElementById("search-btn").addEventListener("click", function () {
  const city = document.getElementById("city-input").value;
  console.log("City entered:", city); // Logs the entered city name
  // Additional code to fetch weather data will be added in later experiments
});
